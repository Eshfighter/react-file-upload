import React from 'react';
import { HeaderComponent, HeaderContents } from './styled/Header';

const Header = () => {
  return (
      <HeaderComponent>
        <HeaderContents/>
      </HeaderComponent>
  );
}

export default Header;
